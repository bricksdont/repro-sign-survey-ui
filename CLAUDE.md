# CLAUDE.md — repro-sign-survey-ui

## What this is

A multi-page metadata annotation tool with two independent tasks: **Reviewing** and **Checking**, plus shared **Datasets** and **Metrics** catalogues. A landing page lets annotators choose which task to work on. Each task has its own overview page listing papers with status badges and a detail page showing the PDF on the left and editable fields on the right. Built for a sign-language NLP survey to track reproducibility metadata (code repos, datasets, metrics) and to verify paper scope.

## Stack

Plain HTML/CSS/JS — no framework, no build step. Node/npm is used only for dev tooling (HTML validation, Playwright tests) — not required to run the app. Don't introduce bundlers or frameworks unless explicitly asked.

## Running

```bash
python3 server.py        # port 8765
python3 server.py 9000   # custom port
# open http://localhost:8765 — redirects to login.html if not authenticated
```

Use `server.py`, not bare `python3 -m http.server`. The custom server adds a `/pdf/<id>.pdf?url=<encoded>` endpoint that fetches PDFs server-side, bypassing both CORS restrictions and `X-Frame-Options: SAMEORIGIN` headers (e.g. OpenReview). Must be served (not `file://`) for the API fetch and proxy to work.

If the direct fetch fails (dead link, paywall), the proxy falls back to fetching `<id>.pdf` from a private Cloudflare R2 bucket, for papers whose PDF has been manually re-hosted there as a preparation step. Requires `boto3` (`pip install -r requirements.txt` — optional for local dev; without it, or without the `R2_*` env vars below set, the fallback is silently skipped and the original fetch error is returned) and four Fly secrets: `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`. No changes to `papers.pdf_url` are needed — the fallback is automatic and keyed purely off the paper's `id`.

"Failure" isn't just a network error or 4xx/5xx status — a `pdf_url` that's a DOI link commonly redirects to a publisher landing page, or gets a WAF/bot-challenge response, both of which can come back as HTTP 200/202 with an empty or HTML body instead of a real PDF. `_looks_like_pdf()` checks the fetched bytes start with the `%PDF-` magic header before accepting a direct fetch as successful; anything else is treated as a failure and falls through to the R2 attempt exactly like a network error would.

## Backend URL

`api.js` checks `window.location.hostname` (the hostname in the browser's address bar) to pick a backend:

| `window.location.hostname` | Backend used |
|---------|-------------|
| `localhost` | `http://localhost:8090` |
| Anything else | `https://repro-sign-survey-backend.fly.dev` |

Override with a URL parameter on any page:

```
http://localhost:8765?backend=remote   # force Fly.io
http://localhost:8765?backend=local    # force local
```

The override is stored in `localStorage` as `pb_backend` the first time it is seen, so it persists across page navigations and logout. It is NOT cleared on logout — it is a routing preference, not a credential.

## JSON export endpoint

`server.py` also serves `GET /export?collection=<papers|datasets|reproductions>&id=<id>`, a thin authenticated proxy onto PocketBase — curlable/scriptable for external tooling, and also what the in-app "Download JSON" button on `paper.html`/`reproduction.html` calls under the hood (see below). This has to live server-side, and `server.py` is the only server-side code in this repo — no Node/Express process exists to put JS logic into instead, and standing one up just for this route would contradict the "no framework, no build step" stack rule.

- **Auth**: requires an `Authorization: Bearer <token>` header — the caller's own PocketBase token, forwarded to PocketBase as-is. No service credential of any kind is used, so the endpoint can only ever see what that token's own session already can (same access as using the app in a browser, no new exposure). Missing header → `401` from `server.py` itself. A syntactically-present-but-invalid/expired token behaves differently: PocketBase's list/view rules for these three collections just require *some* authenticated user (`@request.auth.id != ""`), and an unparseable token is treated exactly like no auth at all — so a full-collection export silently comes back as an empty array, and a by-id lookup comes back as a plain `404` even for an id that demonstrably exists. Confirmed directly against PocketBase (not a `server.py` bug) — it's the same thing a browser would see hitting PocketBase with a stale token, `server.py` just doesn't add its own separate validation on top.
- **`collection` whitelist**: `papers`, `datasets`, `reproductions` only (not `metrics`, `check_papers`, or `users`) — anything else → `400`.
- **`id` semantics differ per collection**, matching how each is already addressed elsewhere in the frontend: for `papers`, `id` is the `paper_id` slug (`filter=(paper_id="<id>")`); for `reproductions`, `id` is the *paper's* `paper_id`, since a reproduction row has no slug of its own (`filter=(paper.paper_id="<id>")`, using PocketBase's relation dot-notation in filters — confirmed this works against the live backend); for `datasets`, `id` is PocketBase's own internal record id (no slug exists for datasets), fetched via a direct GET-by-id. Omitting `id` exports the full collection instead of one record (paginated internally via the same page-until-`totalItems`-is-covered loop `pbGetAll()` uses client-side, so a collection that ever grows past PocketBase's 500-per-page ceiling still exports completely).
- **Expansion**: `papers` → `expand=datasets,metrics`; `reproductions` → `expand=paper.datasets,paper.metrics`, a **double expansion** via PocketBase's dot-notation nested expand — first expanding the `paper` relation (needed so the response carries the readable `paper_id` you queried with; a reproduction's own `paper` field is only ever PocketBase's internal id, and without the expand there'd be no way to correlate the returned record back to the `id` you passed in), then further expanding *that paper's own* `datasets`/`metrics` relations, landing at `expand.paper.expand.datasets`/`expand.paper.expand.metrics` as full inline records rather than bare id arrays — so a reproduction export carries everything about the paper's datasets/metrics (name, license, availability, etc.) without a second request; `datasets` → no expansion needed.
- **Field stripping**: `locked_by`/`locked_at` are removed from every record and, recursively, from every record nested under `expand` at any depth (`_strip_locking()` in `server.py` recurses into `expand` rather than handling just one level) — a paper's expanded datasets/metrics, a reproduction's expanded paper, and now that paper's own further-expanded datasets/metrics (three levels deep, from the double expansion above) all carry their own independent locks, so all of them need stripping, not just the top one or two.
- **Response shape**: a single JSON object for an `id` lookup (`404` if nothing matches), a bare JSON array for a full collection.
- **`id` value validation**: rejects any `id` containing `"` or `\` with `400` before it ever reaches a PocketBase filter string — every real id in this schema is a plain hex/alnum string, so this sidesteps needing to get PocketBase's own filter-string escaping exactly right, rather than risk building an invalid or injectable filter.
- **PocketBase URL**: auto-detected per request from the incoming `Host` header via `_resolve_pb_url()` — `localhost`/`127.0.0.1` → `http://localhost:8090`, anything else → the deployed backend — mirroring `api.js`'s own `window.location.hostname` rule (see "Backend URL" above), just driven by the request instead of a browser location, since `server.py` has none to inspect. This means `/export` "just works" identically whether `server.py` is running locally or deployed, with nothing to remember to set. A `PB_URL` env var is still available as an escape hatch for the rare case a locally-running `server.py` needs to point somewhere other than local — e.g. `PB_URL=https://repro-sign-survey-backend.fly.dev python3 server.py` — and always wins over the per-request auto-detection when set.
- **Load**: negligible at current/foreseeable scale — this relocates work the browser already does today (every stats/overview page calls `pbGetAll()` directly against PocketBase on load) rather than adding new work; the full-collection payloads are well under 1MB of JSON for ~190 papers / ~220 datasets, and the endpoint requires the same auth every other feature already does, so it isn't reachable by anyone who couldn't already pull this data via the browser.

**Why a bare curlable URL alone wasn't enough**: a plain `<a href="/export?...">` link can't attach the `Authorization` header the endpoint requires — browsers never send custom headers on a navigation or link click — and expecting a Slack-SSO user to open devtools and read `localStorage.getItem('pb_token')` themselves is exactly the kind of friction this needs to not have. Two alternatives were considered and rejected: accepting the token as a `?token=` query param (works from a plain link, but leaks the token into `server.py`'s request logging and Fly's log aggregation on every call, plus browser history) and switching the whole app from `localStorage` tokens to cookie-based sessions (would make a bare `/export` visit "just work," but touches every page's auth handling and introduces CSRF considerations the current design doesn't need to think about — disproportionate for one endpoint). Instead: an in-app "Download JSON" button, calling `downloadExport(collection, id)` (`js/api.js`) — a `fetch()` with the header already available from `getToken()`, same mechanism the rest of the app already uses for every other authenticated request, then a `Blob` + `<a download>` to save the response as a file (verbatim the old `dataset-confirmation-overview.js`'s download mechanism, just fetching from `/export` instead of calling PocketBase directly). No new exposure, zero devtools, one click.

- **Placement is `paper.html`/`reproduction.html` only** — each gets a button exporting **just that one record** (`id` set). `dataset.html` deliberately has none: a button there was tried too but dropped as UI clutter, matching the decision to drop the overview-page buttons below — full-collection/single-dataset export is still fully functional server-side (`/export?collection=datasets&id=<id>`), just not surfaced as `dataset.html` UI. The three overview pages (`review-index.html`/`datasets-index.html`/`reproduction-index.html`) deliberately have no button either, for the same reason: a full-collection export via `#export-json-btn` was tried there first but dropped as UI clutter — the overview already shows the full list on screen, so the marginal value of a one-click full-collection download didn't justify the extra button on every filter bar. `paper.html` gets its button next to the existing "Copy Link"/"Status History" buttons (`.copy-link-btn` styling, exports `id=papers[currentIndex].id`, the `paper_id` slug); `reproduction.html` gets one in its own `.detail-secondary-actions` row below the header (rather than sharing the top row with the ◀ ▶ nav, so it doesn't compete for space with or wrap alongside the record-level nav controls), left-aligned, next to a "Copy Link" button — same `.copy-link-btn` styling and `Copied ✓` feedback as `paper.html`'s Copy Link, but stripping `reproduction.html`'s own nav-filter params (`status`/`assigned`/`all_available`/`all_on_modal`) and `q` rather than `paper.html`'s `q`/`status`.
- **`reproduction.html`'s button starts `disabled`**, enabled as soon as the *paper* loads (in `populatePaperInfo()`), not gated on a reproduction row existing yet — clicking it for a paper with no row 404s. `/export`'s own error text ("reproduction not found for that paper") is written for API/curl consumers and doesn't explain why; the in-app click handler recognizes that exact message and swaps in an actionable alert instead ("Nothing to export yet — no reproduction record exists for this paper. Try making any change (e.g. assign yourself, set a status, or add a URL) and saving — that creates the record."), since a reproduction row is created lazily on first save and this is almost always just "nothing saved here yet," not a real error. Any other error message (e.g. a network failure) passes through unchanged.

## File layout

| File | Purpose |
|------|---------|
| `index.html` | Landing page: task selection cards (Reviewing / Review Stats / Datasets / Metrics / Reproducing (Beta)) |
| `review-index.html` | Reviewing overview: paper list with status badges, stats, search/filter |
| `paper.html` | Review detail page: two-panel shell (PDF left, metadata form right) |
| `check-index.html` | Checking overview: check_papers list with Needs Check / Flagged / Checked statuses |
| `paper-check.html` | Check detail page: two yes/no questions (empirical results, SLP scope), flag workflow |
| `datasets-index.html` | Datasets overview: search + 6 filters, wide layout; table of all datasets (name, license, assignees, availability, on Modal, correspondence, URL); rows navigate to detail page |
| `dataset.html` | Dataset detail page: form for name, license, URLs, assignees, availability, comments; edit locking; `?id=<pb-id>` |
| `metrics-index.html` | Metrics overview: table of all metrics (name, URL, comments); rows navigate to detail page |
| `metric.html` | Metric detail page: form for name, URLs, comments; edit locking; `?id=<pb-id>` |
| `stats.html` | Review Stats dashboard: status breakdown, top reviewers, top datasets/metrics, SLP area + sub-area breakdown, answered-fields table; subnav to Dataset/Reproduction stats (issue #103) |
| `dataset-stats.html` | Dataset Stats dashboard: availability/on-Modal/correspondence breakdowns, top assignees, "Used in a Final paper" toggle (issue #103) |
| `reproduction-stats.html` | Reproduction Stats dashboard: reproduction status breakdown, top assignees, all-datasets-available/on-Modal breakdowns (issue #103) |
| `reproduction-index.html` | Reproduction Tracker overview: all finalized papers, search + filters (reproduction status, assigned to me, all datasets available, all datasets on Modal), wide layout; rows navigate to the detail page (issue #110) |
| `reproduction.html` | Reproduction detail page: read-only paper info (ID, title, link to reviewing, a Name/Available/On Modal datasets table) + editable form (assignees, status, URLs, comments); own independent edit lock; `?paper_id=<paper-id-slug>` (issue #110) |
| `login.html` | Login form: authenticates against PocketBase, stores token in localStorage with 24h expiry; also a "Sign in with Slack" OAuth2 button |
| `oauth-redirect.html` | OAuth2 callback page: completes the Slack sign-in code exchange and redirects to `next` |
| `register.html` | Self-service registration page (currently disabled — new registrations are blocked server-side) |
| `js/api.js` | Shared PocketBase client: auto-detected `PB_URL`, `pbGet`, `pbPatch`, `pbGetAll`, `requireAuth`, token helpers, `startOAuth2`/`completeOAuth2`, `downloadExport()`; requests are wrapped with a 10s timeout and network-failure handling (`fetchWithTimeout()`) |
| `js/review/overview.js` | Reviewing overview logic: loads `papers` collection, search/filter/render |
| `js/review/app.js` | Review detail logic: form, PocketBase persistence, edit locking, autocomplete, divider drag |
| `js/check/check-overview.js` | Checking overview logic: loads `check_papers` collection, search/filter/render |
| `js/check/check-app.js` | Check detail logic: form validation, PocketBase persistence, edit locking, divider drag |
| `js/datasets/datasets-overview.js` | Datasets overview logic: loads `datasets` collection, renders clickable table rows |
| `js/datasets/dataset-detail.js` | Dataset detail logic: load by `?id=`, POST/PATCH, edit locking, heartbeat, ◀ ▶ navigation within the filtered selection, self-assign toggle (#108) |
| `js/metrics/metrics-overview.js` | Metrics overview logic: loads `metrics` collection, renders clickable table rows |
| `js/metrics/metric-detail.js` | Metric detail logic: load by `?id=`, POST/PATCH, edit locking, heartbeat |
| `js/stats/stats-shared.js` | Shared stats helpers used by all three stats pages: `tally`, `sortedEntries`, `renderBarSection` (ranked/top-N bars), `renderFixedBreakdown` (fixed-order/fixed-color bars) (issue #103) |
| `js/stats/stats.js` | Review Stats dashboard logic: loads all `papers` (expanded), computes and renders all breakdowns client-side |
| `js/stats/dataset-stats.js` | Dataset Stats dashboard logic: loads all `datasets`, computes and renders availability/on-Modal/correspondence/top-assignees breakdowns (issue #103) |
| `js/stats/reproduction-stats.js` | Reproduction Stats dashboard logic: loads final `papers` (expanded on `datasets,reproductions_via_paper`, same fetch/flatten as `reproduction-overview.js`), renders status/top-assignees/all-datasets breakdowns (issue #103) |
| `js/reproduction/reproduction-overview.js` | Reproduction Tracker overview logic: loads `papers` (expanded on `datasets,reproductions_via_paper`), filters client-side to `status === 'final'`, flattens reproduction fields onto each paper, config-driven `FILTERS` (URL-synced), renders the table |
| `js/reproduction/reproduction-detail.js` | Reproduction detail logic: load by `?paper_id=` (the paper's `paper_id` slug, not its PocketBase id), lazy-create-or-PATCH the `reproductions` row, its own edit locking/heartbeat, self-assign toggle, URL chips, ◀ ▶ navigation within the filtered selection (issue #110) |
| `css/style.css` | Layout, form styles, tag chip styles, overview styles, landing page styles, breadcrumb styles |
| `screenshots/` | README screenshots only |
| `data.json` | Reference seed data; validated by CI (no longer read by the frontend) |
| `Dockerfile` | Container image: Python 3.12 Alpine running server.py on port 8765 |
| `requirements.txt` | Python deps for server.py — currently just `boto3`, for the R2 PDF fallback |
| `fly.toml` | Fly.io app config: app `repro-sign-survey-frontend`, region `fra` |
| `scripts/validate_data.py` | CI: validates data.json schema |
| `tests/smoke.spec.js` | Playwright smoke tests (landing, review overview + detail, check overview + detail, datasets/metrics overview + detail, stats, reproduction tracker overview + detail) |
| `playwright.config.js` | Playwright config; auto-starts server.py for tests on its own dedicated port (8766, never 8765 — see Testing below); `/export` tests need no `PB_URL` override since `localhost:8766`'s Host header already auto-detects to the local backend |
| `package.json` | Dev dependencies (html-validate, @playwright/test); `version` is the single source of truth for the app version (issue #21) — served as a static file and fetched by `index.html` to render the version badge |
| `.github/workflows/ci.yml` | CI: syntax checks, JSON validation, HTML validation, Playwright |

## Key behaviours

- **Auth**: all pages redirect to `login.html` if no valid PocketBase token is found in `localStorage`. The token is stored with a client-side expiry timestamp (`pb_token_expiry`, `SESSION_TTL_MS` in `api.js`) set to 5 days at login — matching the backend's actual `authToken.duration` on the `users` collection (`432000`s = 5 days, confirmed by querying the live collection config directly), rather than the shorter 24h it used before (issue #88), which discarded still-valid tokens early and forced a relogin on nearly every once-a-day visit. This window is fixed at login time, not sliding/refreshed by activity. `getToken()` in `api.js` returns `null` and clears the keys if the token is missing or expired. Using `localStorage` (not `sessionStorage`) means the token is shared across tabs, so copied paper links open without re-login. Two ways in: email/password (`auth-with-password`) and Slack OAuth2; both end by storing the same `pb_token`/`pb_user_id`/`pb_email`/`pb_token_expiry` keys.
- **Landing page** (`index.html`): five task cards — Reviewing and Stats (first row), Datasets and Metrics (second row), Reproducing (Beta) (third row) — plus an account menu. Each card links to its own overview page. All cards are enabled. Checking (`check-index.html`) deliberately has no card here — see below. The "Reproducing" card follows the same gerund-task naming convention as "Reviewing"/"Checking" (its target page is titled the fuller "Reproduction Tracker", same relationship as the "Stats" card → `stats.html`, whose own breadcrumb still says the fuller "Review Stats"); the "(Beta)" suffix flags that this task, unlike the others, is newly launched. The "Stats" card (renamed from "Review Stats", issue #103) points at `stats.html`, the entry point into all three stats pages — see below.
- **Version badge** (issue #21): a small muted `v<version>` link in the bottom-right corner of the landing page, pointing at the matching GitHub release tag. Fetches `package.json` at runtime (served as a plain static file — no build step involved) and reads `.version`; fails silently if unavailable. `package.json`'s `version` field is the single source of truth — bump it with `npm version <patch|minor|major>` when cutting a release, which atomically bumps the field, commits, and creates the matching `vX.Y.Z` git tag, so the badge and the tag can't drift apart. Not currently duplicated onto other pages (each page has its own standalone account-menu markup/JS, so adding it elsewhere means repeating the same small fetch+badge snippet).
- **Breadcrumb navigation**: overview pages show `Home → Reviewing` / `Home → Checking` / `Home → Datasets` / `Home → Metrics` at title-font size; "Home" is a muted grey link, current page is bold black. Detail pages have a `← Back` link returning to the appropriate overview.
- **Reviewing overview** (`review-index.html`): lists all papers from the `papers` collection with ID, title, status badge, and a Review link. Shows counts per status. Search box filters by ID or title (live, substring), with a × clear button (`#search-clear-btn`) that appears once there's text to clear and resets the search on click; same on `check-index.html`. Status filter pills narrow to a specific status. "Review Next →" navigates to a random `needs_review` paper that isn't currently locked by another user (issue #71) — `allPapers` already carries `locked_by`/`locked_at` from the initial fetch, so this needs no extra request; disabled with "No unlocked papers left to review" once every remaining `needs_review` paper is locked.
- **Checking overview** (`check-index.html`): lists all papers from the `check_papers` collection. Status values: `needs_check`, `flagged`, `checked`. "Check Next →" navigates to a random `needs_check` paper that isn't currently locked by another user (issue #71), same as Review Next. **Deliberately internal/unlisted for now**: its landing-page card slot was given to Review Stats (moved up to the first row), which in turn freed the third row for the Reproduction Tracker (previously the unlisted one) to get a card instead — reachable only via a direct link to `check-index.html`, fully functional either way.
- **Two independent collections**: `papers` and `check_papers` are separate PocketBase collections with independent paper sets (a paper may appear in one, both, or neither). The frontend never mixes them.
- **PDF viewer**: native browser `<iframe>` routed through the local proxy (`/pdf/<id>.pdf?url=...`). Text selection, zoom, and all native controls work. No pdf.js. In Checking, the left panel shows the paper's `abstract` (plain text) instead when present — the screening-pipeline batch behind `check_papers` migration 9 ships abstracts but not always a usable `pdf_url`; papers without an `abstract` still fall back to the PDF iframe.
- **Paper navigation**: ◀ ▶ buttons step through papers. URL updates via `history.replaceState` (`?id=<paper-id>`), so every paper has a stable direct link. "Copy link" button in the header copies a plain `?id=<paper-id>` link to clipboard — deliberately built from `papers[currentIndex].id` rather than `window.location.href`, so it never carries the `?q=`/`?status=` nav-filter params described below; a shared/copied link should stay simple and always point at the full collection for whoever opens it.
- **Filtered navigation subset via URL params** (issue #75): `review-index.html`/`check-index.html` keep the active search text and status pill mirrored into their own URL (`?q=<search>&status=<status>`, via `history.replaceState`, params omitted at their default so an unfiltered view keeps a clean URL) — `buildFilterQuery()`/`syncURL()` in `overview.js`/`check-overview.js`. This makes the current view bookmarkable/shareable and is restored on load (`init()` reads `?q=`/`?status=` back into the search box and status pill), so returning via `paper.html`'s Back link lands on the same filtered view; the search box also has a × clear button (`#search-clear-btn`) that appears once there's text to clear. Clicking a row — or the explicit "Review →"/"Check →" link at its end, whose `href` is built with the same `buildFilterQuery()` output rather than left static (issue #91: the row's click handler explicitly ignores clicks on `<a>` elements, so the link's own `href` has to carry the filter itself or a click straight on the link silently drops it) — carries the same `q`/`status` params into `paper.html`'s URL; there, `?id=`/`?q=`/`?status=` are parsed on load and `computeNavOrder()` recomputes the matching subset of paper IDs from the already-loaded `papers` array (same predicate as the overview's `applyFilters()`) — a live recomputation, not a frozen ID list, so it also works for a link shared with someone else or opened in a new tab. ◀ ▶ (`loadAdjacentPaper()`), the `X / Y` counter, and the Back link's `href` (`updateBackLink()`) all stay within this subset; `id`/`q`/`status` are kept in sync in the URL via `history.replaceState` on every paper load (`buildPaperUrl()`). "Copy Link" is the one exception — it always strips `q`/`status` (see above). If the loaded paper isn't in the computed subset (a stale/non-matching `q`/`status`, or Finalize & Next landing outside it — that flow is intentionally left as full-collection, unfiltered, per its existing logic), navigation self-heals by dropping `q`/`status` and falling back to the full collection.
- **Reviewing status workflow** (issue #47): four statuses — `needs_review`, `final`, `flagged`, `rejected`.
  - **Autosave**: every field change (radio, chip add/remove, textarea, N/A toggle, or blurring a pencil-edit field) schedules a debounced save 1 second after the last change (`scheduleAutoSave()`/`runAutoSave()` in `app.js`). Autosave never changes `status` or `finalized_by` — it only persists the editable field values, whatever the current status. A `Saving…` / `Saved ✓` / `Save failed` indicator appears next to the action buttons — `Saved ✓` stays on screen (no auto-hide) once shown, so it doesn't quietly vanish while the reviewer is still working through the form; it's only replaced once another save starts (`Saving…`) or the paper changes (cleared via `hideFooterMessages()` in `loadPaper()`). `persistWithIndicator()` (shared by autosave and Finalize) enforces a 500 ms minimum display time for `Saving…` — against a fast/local backend the PATCH can resolve in a handful of milliseconds, which made the indicator flash too briefly to actually notice the state change once `Saved ✓` was already showing continuously.
  - **Network-failure handling**: `pbGet`/`pbPatch` (`js/api.js`) wrap every request in a 10-second timeout (`fetchWithTimeout()`, via `AbortController`) and catch the network-level failure `fetch()` throws when the backend is unreachable (e.g. stopped) — neither case is an HTTP error status, so `res.ok` alone can't detect them. Without this, an unreachable/hung backend during autosave left the indicator stuck on `Saving…` forever, since the uncaught rejection aborted `runAutoSave()` before it reached the code that would set `Save failed`. `pbPatch` now resolves `{ ok: false, status: 0 }` on either failure — the same shape callers already handle for an HTTP error — so the existing `Save failed — will retry on next change` state is reachable; `pbGet` throws the same kind of `Error` it already throws for an HTTP failure, rather than letting a different, uncaught `TypeError` escape.
  - Pending edits are flushed immediately (not just debounced) before switching papers (◀ ▶, Finalize & Next) and folded into the same keepalive PATCH that releases the edit lock on tab close/navigation (`beforeunload`), so an edit is never silently lost.
  - **Finalize / Finalize & Next** (replacing the old Save / Save & Next) → the only actions that set `status: final` and stamp `finalized_by` with the reviewer's email. Disabled (with a hover tooltip listing what's missing) unless every required field is filled: Title, Year, Peer-Reviewed, Code Repositories (chip or N/A), Datasets (≥1), Metrics (≥1), Area of SLP (≥1), Ranking, Copied Baseline Scores, Human Evaluation, What to Reproduce, Compute Requirements (text or N/A), Textual Conclusion, Ethical Concerns. Also disabled while the paper is `flagged` or `rejected` (tooltip explains to clear/revert first). Finalize & Next then advances to the next `needs_review` paper, skipping flagged/rejected/final.
  - Flag → opens a dialog to choose/enter a reason; stores `status: flagged` + `flag_reason`.
  - Reject → opens a dialog to choose/enter a reason (presets: "not on SLP", "no quantitative experiments", "not published 2017–2026", "not in English", "no full text PDF", "requires specialized hardware" (issue #67), or free text); stores `status: rejected` + `rejection_reason`.
  - Flag and Reject buttons disable each other (clear/revert first).
  - "Clear flag" / "Revert rejection" / "Revert to needs review" link appears next to the badge to reset status, explicitly clearing `flag_reason`/`rejection_reason` to `''` in the same request (issue #63).
  - Rejection/flag reason is folded into the badge text (`⚑ Flagged · <reason>`) and shown as a tooltip.
  - **Attribution**: `finalized_by` (`papers` migration 1, formerly a nonexistent `reviewed_by`) is set only by Finalize / Finalize & Next, to the logged-in reviewer's email; shown next to the status badge (`by <email>`) only once the paper is `final`. Flagging/rejecting does not stamp its own dedicated field — there's no `flagged_by`/`rejected_by` field in the backend — but every transition, flag/reject included, is still attributed via `status_history`'s `by` field (see Status History below), which is what Review Stats' Top Reviewers section reads from.
  - **Status History** (issue #20): a "Status History" button next to "Copy link" opens a dialog listing every status transition, newest first — `<email> changed status from <Before> to <After>` plus a localized timestamp — sourced from `papers.status_history` (`papers` migration 1, a JSON array of `{by, before, after, when}`). Appended to inside `persistPaper()` whenever `extra.status` differs from the paper's current status, so it captures Finalize, Flag, Reject, and Clear/Revert, but never autosave (which never overrides `status`). Empty state: "No status changes recorded yet."
- **Stats pages** (issue #103): the original single Review Stats dashboard is now three separate pages — Reviewing (`stats.html`), Datasets (`dataset-stats.html`), Reproduction (`reproduction-stats.html`) — tied together by a **subnav** of pill-style links (`.stats-subnav .filter-btn`, reusing `.status-filters`/`.filter-btn`'s existing look) at the top of each, one page hardcoded `active` per page since this is real page navigation between three separate URLs, not a client-side tab toggle — no new routing pattern needed, consistent with how every other concern in the app (Reviewing/Checking/Datasets/Metrics/Reproducing) already gets its own dedicated page. `.filter-btn` had only ever been applied to `<button>`s before this; `a.filter-btn { display: inline-block; text-decoration: none; }` makes it render correctly as a link too (same pattern as the existing `a.stat-bar-label` override). The landing-page card was renamed "Review Stats" → "Stats" to match (still links to `stats.html`, the entry point). `tally`/`sortedEntries`/`renderBarSection` (ranked/top-N bars) and a new `renderFixedBreakdown` (fixed-order/fixed-color bars, generalizing what was a hardcoded `renderStatusBreakdown`) were extracted into `js/stats/stats-shared.js`, loaded before each page's own script, rather than tripling the same ~70 lines of logic across three files. **Gotcha found while building the two new pages**: `tally()` treats an empty-string key as "no value, skip" — fine for ranked bar sections, since an empty label wouldn't be meaningful there, but wrong for a fixed breakdown where `""` is itself a legitimate, common state (dataset availability/on-Modal unanswered, correspondence not contacted, reproduction not started) — passing `""` straight through silently dropped every record in that state from the total. Fixed by mapping those states to a distinct non-empty tally key (`'unanswered'`, `'not_contacted'`, `'not_started'`) before tallying, matching the same `key || 'sentinel'` pattern `renderStatusBreakdown` already used for `papers.status` (`p.status || 'needs_review'`) — the bug only showed up on the two *new* pages because that page happened to sidestep it by having a real fallback value in the first place.
- **Review Stats dashboard** (`stats.html`, issue #54) — named to distinguish it from any future Checking-task stats: loads every record from the `papers` collection (expanded on `datasets`/`metrics`) and computes all breakdowns client-side — no backend aggregation endpoint. Sections: total paper count; status breakdown (needs_review/final/flagged/rejected) as colored bar rows; top reviewers (issue #86 — every status-change entry across every paper's `status_history`, tallied by `by` and summed regardless of transition type, so Finalize/Flag/Reject/Clear-Revert all count — not just `papers.finalized_by`, which only credited Finalize and left flag/reject work, equally real reviewing effort, uncredited; needs no backend changes since `status_history` already records `{by, before, after, when}` for every transition); top datasets (each with an Available/Not available badge sourced from the dataset's own `available` field, via the existing `expand=datasets` on `papers` — `availabilityBadge()` always wraps the badge slot in a fixed-width `.stat-bar-badge-slot`, even when a dataset's availability is unanswered and has no badge to show, leaving the slot empty rather than omitting it; otherwise that row's bar track would start further left than rows that do have a badge. This keeps every row's track left-aligned regardless of badge text width or absence) and top metrics (bars link to the matching `dataset.html`/`metric.html` in a new tab, capped at the top 10 with a "+ N more" note; the label renders in the same accent blue as `.review-link` — at rest, not just on hover — so it reads as clickable, distinct from the plain-text labels in sections like Top Reviewers that don't link anywhere); a breakdown of `area_of_slp` usage (top 12); a breakdown of `sub_area_of_slp` usage (top 12, issue #103, same pattern one field over — a version grouped by main area was tried and reverted: matching each sub-area value against the known `RECOGNITION_SUB_AREAS`/`TRANSLATION_SUB_AREAS` pools dumped anything from any other main area, e.g. Spotting/glossing, into a generic "Other" bucket, which wasn't the intended behavior and would have needed a fully dynamic per-main-area grouping instead — reverted to the plain flat list rather than build that); and a compact table of Yes/No/N-A/Unanswered counts for Peer-Reviewed, Ranking, Copied Baseline Scores, Human Evaluation, and Ethical Concerns. No charting library — bars are plain CSS width percentages, consistent with the "no framework" rule.
- **Dataset Stats dashboard** (`dataset-stats.html`, issue #103): loads every record from the `datasets` collection plus every `papers` record (`Promise.all([pbGetAll('datasets'), pbGetAll('papers')])`), cross-referencing them once into a `hasFinalPaper` flag per dataset — same approach as `datasets-overview.js`'s own `hasFinalPaper` computation. Sections: total dataset count; Availability breakdown (Yes/No/Unanswered); On Modal breakdown (same shape); Correspondence breakdown (Got reply/Awaiting reply/Not contacted); **Top Assignees** — `tally()` over every dataset's `assignees` array flattened with `.flatMap()`, same `renderBarSection` as Top Reviewers (no link — an email isn't a page). A **"Used in a Final paper" toggle** (`<select id="filter-final-paper">`, same `.filter-select`/`.filter-bar` markup as `datasets-index.html`'s own Final-paper filter, defaulting to "All") narrows the dataset pool every section on the page is computed from, including the summary count — a deliberate difference from `datasets-index.html`'s `#stats-row`, which always reports *unfiltered* totals no matter what the table below it is filtered to; here the toggle changes what's being measured, not just which rows are visible, so the summary has to move with it or the two would read as contradictory. `applyFilter()` re-renders every section from `allDatasets` (filtered or not) on toggle change, syncs the choice into the URL (`?final=only`, omitted at its "all" default, same omit-at-default convention as every other filter in the app) via `history.replaceState`, and restores it on direct navigation.
- **Reproduction Stats dashboard** (`reproduction-stats.html`, issue #103): data fetch/flatten is a direct copy of `reproduction-overview.js`'s `init()` — `pbGetAll('papers', '&expand=datasets,reproductions_via_paper')` filtered to `status === 'final'`, `extractRepro()`'s back-relation-is-an-object normalization, flattened into `reproStatus`/`reproAssignees`/`allDatasetsAvailable`/`allDatasetsOnModal` per paper (same duplication convention `reproduction-detail.js` already uses for this exact logic). Sections: total final-paper count; Reproduction Status breakdown (Not started/In progress/Finished); **Top Assignees** (`tally()` over every final paper's `reproAssignees`); All Datasets Available breakdown (Yes/No); All Datasets On Modal breakdown (Yes/No).
- **Reproduction Tracker** (`reproduction-index.html` + `reproduction.html`, issue #110 — replaces the earlier "Dataset Confirmation Tracker", issue #90, which only tracked whether a paper's datasets were confirmed available; this tracks the actual reproduction *effort*): lists **every** `status === 'final'` paper, same to-do-overview rationale as its predecessor. Backed by a separate `reproductions` PocketBase collection (`repro-sign-survey-backend` PR #62) rather than fields on `papers` — see the dedicated bullet below for why. `js/reproduction/reproduction-overview.js`'s `init()` fetches `pbGetAll('papers', '&expand=datasets,reproductions_via_paper')`, filters to `status === 'final'`, and flattens each paper's reproduction state onto it once: `reproStatus` (`''`/`in_progress`/`finished` — `''` covers both "no reproduction row exists" and a row with an explicitly empty status, since the app treats them identically everywhere), `reproAssignees`, `allDatasetsAvailable`/`allDatasetsOnModal` (`true` only if the paper has ≥1 dataset and every one of them is `available`/`on_modal === 'yes'`). `extractRepro()` normalizes the back-relation expand, which empirically returns a **plain object** for `reproductions_via_paper` (not an array, despite that being PocketBase's general documented shape for back-relations — confirmed against the live backend; `reproductions.paper` is unique, so there's at most one match either way) — defensively handles both shapes in case that ever changes. Same config-driven `FILTERS` array shape as `datasets-overview.js` (`{param, elementId, default, match}`), four entries: Reproduction status (All/Not started/In progress/Finished), Assigned to (All/Only assigned to me/Assigned to anyone/Assigned to nobody — the "anyone"/"nobody" pair added after initial ship, matching on `reproAssignees.length > 0`/`=== 0` rather than a specific email), All datasets available (All/Yes/No), All datasets on Modal (All/Yes/No) — plus the search box (paper ID or title, live substring). Same URL-sync/`.active`-highlight/Clear-filters wiring as `datasets-index.html` (issue #106). Table: truncated Paper ID, Title, Assignees (first email only, same convention as `datasets-overview.js`'s Assignees column, issue #108), Reproduction status badge, All Datasets Available badge, All Datasets On Modal badge, "Details →" (`col-action`). Unlike its predecessor, the table has **no per-paper dataset chips column** — that per-dataset detail moved to the detail page instead (below); showing it here would duplicate the two new aggregate badge columns without adding information at the overview-scanning level. Row click and "Details →" both navigate to `reproduction.html` in the **same tab**, deviating from the old page's new-tab convention — this page is now an active editing workflow (assign yourself, update status), not a static reference list, matching why `datasets-index.html → dataset.html` is also same-tab. Stats row: `"<total> final papers · <notStarted> not started · <inProgress> in progress · <finished> finished"`, same `.stat`/`.stat-sep` markup as `datasets-index.html`. No JSON export (the predecessor's "Download JSON" was dropped, not carried over). Reachable from a landing-page card (third row, full width, titled "Reproducing (Beta)" — see Landing page, above).
- **Reproduction detail page** (`reproduction.html`, issue #110): URL-keyed by the **paper**'s `paper_id` slug (`?paper_id=<paper-id-slug>`), not by the reproduction row's own id or the paper's internal PocketBase id, since a paper may not have a reproduction row yet — rows are created lazily on first save (see below) — and every other cross-link in the frontend (`paper.html`, `review-index.html`, "Used in Papers") already references a paper by `paper_id`, never its internal PocketBase id; using `paper_id` here too avoids leaking that internal id into a public URL. Since PocketBase's REST API only fetches a single record by its own internal id, not by an arbitrary unique field, loading is a filtered list query instead of a direct GET-by-id: `pbGet('/api/collections/papers/records?filter=(paper_id="<slug>")&expand=datasets,reproductions_via_paper&perPage=1')`, taking `items[0]`. That single request loads everything needed: a read-only **Paper** block (`.stats-section`, same bordered-card style as `dataset.html`'s "Used in Papers") showing ID (the paper's `paper_id`), Title, an "Open in Reviewing →" link to `paper.html?id=<paper_id>` (new tab, mirrors the "Used in Papers" cross-workflow-link convention), and a "Datasets" heading with a small read-only table (Name/Available/On Modal — issue #110 follow-up, replacing an initial chip-list design that could only convey one of the two dimensions at a glance via chip color) listing each of the paper's datasets, name linking to `dataset.html?id=<id>` in a new tab, with the same `avail-badge`/`avail-yes`/`avail-no` badge markup `datasets-overview.js` already uses for its own Available/On Modal columns. Below that, an editable form: **Assignees** (verbatim copy of `dataset.html`'s self-assign-only toggle pattern, issue #108 — `toggleAssignMe()`/`renderAssigneeChips()`/`updateAssignMeButton()`, `.text-action-btn` styling), **Reproduction status** (3-way radio, most-progressed state first: Finished `finished` / In progress `in_progress` / Not started `""`), **URLs** (verbatim copy of `dataset.html`'s `urlChips` add/remove pattern), and **Comments** (plain textarea — in the `reproductions` schema but not explicitly named in issue #110; included for consistency with every other collection having one). ◀ ▶ navigation steps through the same filtered subset the paper was opened from (`computeNavOrder()` duplicating `reproduction-overview.js`'s `FILTERS`, same live-recomputation approach as `dataset-detail.js`/`app.js`), with the same `isDirty`-guarded `confirm()` before navigating away with unsaved changes. Save: PATCHes the existing `reproductions` row if one was found via the expand, or POSTs a new one (`{ paper: <paper's internal PocketBase id>, assignees, status, url, comments }` — the relation field always needs the internal id, unlike the URL) if not, then immediately acquires its lock — same two-step create-then-lock sequence as `dataset.html`'s `save()`.
- **`reproductions` collection and independent locking** (`repro-sign-survey-backend` PR #62, issue #110): one row per paper (`paper` relation, required+unique, `cascadeDelete: true`), `assignees` (json array of email strings, same self-assign-only convention as `datasets.assignees`), `status` (select, `in_progress`/`finished` — `""` means "not started," whether because no row exists at all or a row exists with an unanswered status), `url` (json array, unvalidated, same as `datasets.url`), `comments`, and its **own** `locked_by`/`locked_at` — completely independent of the paper's own review lock. This is a deliberate redesign: an earlier version (PR #61, superseded) put `reproduction_*` columns directly on `papers`, but PocketBase access rules are per-*record*, not per-field — the existing review-lock rule (`locked_by = "" || locked_by = @request.auth.id`) gates every write to a paper, so a second lock field on the same record couldn't enable independent editing; whoever held the review lock also blocked reproduction edits, and even acquiring the reproduction lock was itself a write that hit the same wall. A separate collection removes the conflict entirely rather than working around it — verified with two real users in the backend PR: reviewer A's paper lock blocks reviewer B from editing the *paper*, but doesn't block B from creating or editing *that paper's reproduction row*, and vice versa. `reproduction-detail.js`'s `acquireLock()`/`releaseLock()`/`startHeartbeat()`/`isLockExpired()` are an exact copy of `dataset-detail.js`'s (30 min expiry, 60s heartbeat, `beforeunload` keepalive release, failed-PATCH → `setReadOnly(true)`), just pointed at `/api/collections/reproductions/records/<id>` instead. Since `reproductions.deleteRule` is unset (no API delete for a normal user, by design — a reproduction row is meant to persist once work has started), a test or manual mistake that creates a row can't be un-created; the correct fix is resetting every field back to its empty/default value, which the app treats identically to "no row exists" everywhere (`reproStatus`/`reproAssignees` both fall back the same way either way).
- **Checking status workflow**: three statuses — `needs_check`, `flagged`, `checked`.
  - Save / Save & Next → marks as `checked` (only if currently `needs_check`; flagged status is preserved).
  - Save & Next → advances to the next `needs_check` paper; falls back to `check-index.html` if none remain.
  - Flag → opens a dialog to choose/enter a reason; stores `status: flagged` + `flag_reason`.
  - "Clear flag" / "Revert to needs check" link resets to `needs_check`, explicitly clearing `flag_reason` to `''` in the same request (issue #63).
  - **Form validation**: `is_sign_language_processing` must always be answered; `has_empirical_results` is only shown (and required) when `is_sign_language_processing` is `yes`. Both must be answered before Save / Save & Next become active.
  - **Attribution**: every save (`persistPaper()`) writes the logged-in reviewer's email to `checked_by` (`check_papers` migration 10), regardless of status.
- **Pre-filled fields** (title, year, venue in Reviewing; title, year, language in Checking): shown read-only, with a pencil button that makes the field editable; blur or Enter returns to display mode. In Checking, Title stays read-only — Year and Language are both editable this way.
- **Checking source metadata** (from `check_papers` migration 9): `language` shows next to Title/Year and is reviewer-editable like Year. `filters` (automated screening-pipeline eligibility checks, e.g. `area`/`approach`) pre-selects the SLP and empirical-results radios when the reviewer hasn't answered yet — an "LLM suggested" badge marks these until the reviewer confirms by clicking or saving. `filter_explanations` is in the schema but not currently surfaced in the UI.
- **Tag fields** (code repos, datasets, metrics — Reviewing only): chip list with × removal; inline input + Add button (also triggered by Enter). Datasets and metrics both have live autocomplete dropdowns backed by their respective PocketBase collections (`datasets`, `metrics`), with an "Add … as new … to the database" option for inline creation. Code repo chips are clickable links. Dataset and metric chips show a ↗ link that opens the corresponding detail page in a new tab.
- **Unnamed dataset convention** (issue #92, replacing an earlier shared-"custom"-placeholder design from issue #85): for a paper whose dataset has no name (e.g. collected in-house for the study, unpublished), a "Create unnamed dataset" button next to the Datasets field (`createUnnamedDataset()` in `app.js`) creates a brand-new `datasets` record and attaches it to the paper in one step. Each click creates its own record — the name is generated as `unnamed-<record id>`, so every instance is distinguishable, unlike the earlier design where a single shared "custom" record collapsed every unnamed dataset (across every paper) into one indistinguishable entry with no way to tell them apart or record per-instance detail. Since the record's own PocketBase id isn't known until after creation, the record is first created with a temporary unique placeholder name (`unnamed-pending-<timestamp>-<random>`, satisfying the collection's `required`+`min:1`+unique-index-on-`name` constraints) and immediately renamed via a follow-up PATCH once the real id is known. The Datasets field's info tooltip points annotators to the resulting chip's own ↗ detail-page link to add whatever detail is available (comments, license, URL, availability — the same fields any other dataset record has) directly on that record, rather than in the paper's general Comments field. Comments is never conditionally required by this — it stays fully optional in every case, same as any other free-text field.
- **N/A confirm toggles** (issue #44): Code Repositories and Compute Requirements each have a toggle button — "I confirm that there are no code repositories available." / "...compute requirements were not specified in the paper." Clicking it when the field is empty disables the input/add-button and marks the field as confirmed-N/A; clicking again reverts to the normal empty, editable state. The button itself is disabled whenever the field already has content, so you can't confirm N/A over existing chips/text. When toggled on, the saved value is the literal string `"N/A"` (for `code_repos`, replacing the usual array).
- **Area of SLP** (issue #22, redesigned in issue #43): chip list with × removal, same tag-input pattern as datasets/metrics — inline input + Add button (also triggered by Enter), with a live autocomplete dropdown suggesting from a fixed list of 12 SLP sub-areas (Translation, Recognition, Segmentation / tokenization, Alignment, Signing detection, Generation / production, Unsupervised / representation learning, Spotting / glossing, Transcription, Language identification, Retrieval, Avatar systems). Unlike datasets/metrics, this isn't backed by a PocketBase collection — the suggestion list (`KNOWN_SLP_AREAS`) is a hardcoded frontend constant, and any typed value (including ones not in that list) can be added as a chip. Persists to `papers.area_of_slp` as a plain array of strings.
- **Sub-area of SLP** (issue #101): a second chip field directly below Area of SLP, same tag-input pattern, backed by `papers.sub_area_of_slp` (`repro-sign-survey-backend` PR #54 — a JSON array of free-form strings, no fixed enum server-side, mirroring `area_of_slp`). Its autocomplete suggestions depend on the paper's *current* Area of SLP selection (`computeSubAreaSuggestionPool()` in `app.js`, recomputed on every keystroke/focus rather than cached): if Area of SLP includes the exact area name "Recognition" (case-insensitive, not a substring match — a custom chip like "Isolated Sign Recognition" would not count), the dropdown suggests `RECOGNITION_SUB_AREAS` (Isolated/Continuous fingerspelling images/videos, Isolated/Continuous signing videos); if it includes "Translation", it suggests `TRANSLATION_SUB_AREAS` (Gloss-to-text, Text-to-gloss, Video-to-text, Text-to-video, Pose-to-text, Text-to-pose); both pools are concatenated if Area of SLP includes both. If Area of SLP includes neither (or is empty), the field stays visible with an empty suggestions dropdown — deliberately not hidden, since the field has no fixed enum and still accepts free text either way. Existing Sub-area chips are never retroactively removed when Area of SLP changes afterward; only the suggestion pool for newly-typed values updates. **Not required for Finalize** — deliberately absent from `REQUIRED_FIELD_LABELS`. Info tooltip (same `.info-btn`/`.info-popup` pattern as other fields): "Optional field. Select at least one area above to display potential sub-area labels."
- **Additional Reviewing fields** (issue #22): `main_experiment_has_ranking`, `copied_scores`, and `includes_human_evaluation` — yes/no radios, initially unanswered. `what_to_reproduce` and `compute_requirements` — free-text textareas with an info tooltip explaining what to enter; `textual_conclusion` — free-text textarea for the paper's main conclusion. These persist to the `papers` collection alongside the existing fields.
- **Ethical concerns field** (issue #36): "Potential Ethical Concerns" — `potential_ethical_concerns` — yes/no radios at the bottom of the Reviewing form, optional (can be left unanswered), with an info tooltip explaining what counts as an ethical concern (e.g. derogatory / ableist language).
- **Comments field** (`papers.comments`, `papers` migration 8): free-text textarea, the very last field in the Reviewing form (after Textual Conclusion), always optional — with an info tooltip discouraging overuse: it's for genuine elaboration, not a way to defer/hedge a decision that should instead go through Flag. Per-dataset detail (e.g. for an unnamed dataset) belongs on that dataset's own record instead — see the unnamed dataset convention above.
- **Info tooltips** (`.info-btn`/`.info-popup`, Reviewing and Checking): anchored from the icon's left edge, extending rightward — not centered. `.meta-panel` has `overflow-y: auto`, which per the CSS Overflow spec implicitly computes `overflow-x` as `auto` too (not `visible`), clipping anything that pokes out past the panel's own left/right edge; most field labels sit near the panel's left side, so a centered popup routinely got clipped against that edge (and visually looked obscured by the PDF viewer, since that's what's behind it). Anchoring left avoids needing to escape the panel's boundary in the first place.
- **Peer-Reviewed field** (issue #37): three radios — Yes / No / N/A — instead of the original Yes/No pair. `peer_reviewed` is stored as the string `"yes"` / `"no"` / `"na"` (previously a bare bool); `populateForm` still recognizes legacy `true`/`false` values for backward compatibility with existing records.
- **Datasets catalogue** (`datasets-index.html`): table listing all records from the `datasets` collection (name, license, availability badge, first URL). Clicking a row navigates to `dataset.html?id=<pb-id>`. "+ Add Dataset" navigates to a blank `dataset.html`. The detail page (`dataset.html`) has fields for name, license, URLs (chip list), assignees, availability radio, "On Modal" radio, "Correspondence" radio, and comments, with edit locking and a `← Back` link.
- **Assignees** (`datasets.assignees`, issue #108 — `repro-sign-survey-backend` PR #60): self-assign-only, no picker of other users. A normal reviewer's PocketBase token can only list/view their *own* `users` record (`listRule`/`viewRule` both `id = @request.auth.id`), so a dropdown of tool users to assign was never feasible without loosening backend permissions; the field is instead a plain `JSONField` array of email strings (not a Relation into `users` — which would default existing rows to `[]` and need no expand/id-resolution, but was dropped in favor of the simpler string array once the design settled on self-assign-only) defaulting to `[]`. Existing assignees render as plain, non-removable `.chip`s (email text only — no × button); the only way to change membership is a single "Assign myself" / "Remove myself" toggle (`.text-action-btn`, the same plain-hyperlink style as "Create unnamed dataset" in `paper.html`, not the boxed N/A-toggle style) that adds/removes only `getEmail()`'s own email (`toggleAssignMe()` in `dataset-detail.js`). Disabled if `getEmail()` is falsy. Positioned right after URLs, before Available. `datasets-index.html`'s table also gained an **Assignees** column (right after License, before Available) showing only the *first* assignee's email even when there are several — a compact glance, not the full list, which still lives on the detail page's chip list — plus a sixth filter, **Assigned to** (`filter-assigned`, `all`/`mine`), narrowing to datasets whose `assignees` includes the current user's own email; it slots into the same config-driven `FILTERS` array described below (#106), so it gets URL-sync, `.active` highlight, and Clear-filters handling for free.
- **Datasets overview filtering** (issue #106): `datasets-index.html`'s `<div class="overview">` carries an extra `overview-wide` class (`max-width: 1400px`, vs. the shared 860px every other overview/detail page still uses) so the search box + five `<select>` filters fit in one row instead of wrapping — scoped to this one page since no other page asked to be wider. The filters: Availability, On Modal, Correspondence (each All/value/value/Unanswered-or-not_contacted, mirroring the detail page's own radio options), Orphans (All / Orphaned only / Hide orphaned — "orphaned" = referenced by zero papers), and Final paper (All / Used in a Final paper). All filters AND together. The table itself gained two columns, **On Modal** and **Correspondence** (same badge rendering as Available — `yesNoBadge()`/`correspondenceBadge()` in `datasets-overview.js`, green/red/amber, "—" for unanswered/not-contacted), specifically so filtering is self-verifying: narrowing to e.g. Correspondence = "Awaiting reply" is only useful if the remaining rows visibly show that value, not just a shorter list. `#stats-row` (`renderStats()`) reports these same new dimensions unfiltered, alongside the pre-existing total/available counts: `"<total> datasets · <avail> available · <onModal> on Modal · <usedInFinal> used in a final paper · <contacted> contacted · <gotReply> got a reply"` — `contacted` counts any `correspondence` other than `""` (covers both `contacted_waiting` and `contacted_got_reply`, so it's always ≥ `gotReply`). Built with the *exact* markup pattern `review-index.html`'s stats row already uses — each stat is `<span class="stat"><span class="stat-num">N</span> label</span>`, separators are bare `<span class="stat-sep">·</span>` — rather than plain bolded-number text with literal `" · "` spacing (an earlier version of this row tried that first). `.stats-row` is `display: flex; gap: 10px`, and a flex container wraps every contiguous run of inline content — including bare text — into its own anonymous flex item; without the `.stat` wrapper grouping each number with its own label, `gap` lands between the number and its label too, not just between whole stat groups (too-wide number/label spacing), and separators built from literal text spaces around the glyph rendered visibly asymmetric (the middle-dot sits left-of-center in its own box in this font) in a way pure-`gap` spacing between elements doesn't. `init()` cross-references every dataset against `pbGetAll('papers')` once on load, computing `paperCount` and `hasFinalPaper` per dataset (same array-membership approach as `dataset.html`'s "Used in Papers" and Review Stats — no backend relation-filter query). **Deliberately config-driven** rather than one branch per filter: a `FILTERS` array in `datasets-overview.js` (`{param, elementId, default, match}` per filter) drives `applyFilters()`, `buildFilterQuery()`, and URL restoration generically, so adding a new filter later — including, per the issue, eventually extending `review-index.html` the same way — means adding one array entry and one `<select>`, not touching filtering/URL-sync logic in four places. "Showing X of Y datasets" (`#results-count`, same styling as `review-index.html`'s) appears once any filter or the search box is active, hidden otherwise. Each `<select>` set away from its "all" default gets an `.active` highlight (`.filter-select.active`, same solid accent-blue treatment as `.filter-btn.active`) — toggled in `applyFilters()` off the same `filterValues`/`f.default` comparison already used to decide the result-count and URL — so which of the five dropdowns are actually narrowing the list is visible at a glance without reading each one. A "Clear filters" button (right-aligned via `.filter-bar-right`, same pattern as the Dataset Confirmation Tracker's "Download JSON") resets the search box and every `<select>` back to its default and re-runs `applyFilters()`; it's disabled whenever nothing is active (same `isFiltered` check that also drives `#results-count`), so it can't be clicked as a confusing no-op. Active filters are mirrored into the URL (`?q=&available=&on_modal=&correspondence=&orphan=&final=`, each omitted at its "all" default) via `history.replaceState`, carried into `dataset.html`'s URL by both the "Details →" link and the row click (computed once per render pass, like `review-index.html`'s `qs`), and restored by every link back to `datasets-index.html` — `dataset.html` has two of them, the explicit "← Back" link and the "Datasets" breadcrumb crumb, both updated together by `updateBackLink()` in `dataset-detail.js` (reading the same params back off `dataset.html`'s own URL), since either one silently drops the filter if left static — the same round-trip pattern as `review-index.html ↔ paper.html` (issue #75).
- **Dataset detail ◀ ▶ navigation** (issue #106): steps through the same filtered/searched subset the dataset was opened from, mirroring `paper.html`'s ◀ ▶ (issue #75) — a `dataset-counter` span ("N / M", or "—" for a new/unsaved record not in any nav order) plus prev/next buttons, disabled at the ends, reusing `paper.html`'s `.paper-nav` CSS directly. `computeNavOrder()` in `dataset-detail.js` duplicates `datasets-overview.js`'s `FILTERS` array/predicates and its own `pbGetAll('papers')` cross-reference for Orphans/Final-paper, so `navOrder` reproduces the exact subset from live data — not a frozen ID list — same approach as `app.js`'s `computeNavOrder()`. **Architectural difference from `paper.html`**: `paper.html` swaps records in-place (one shared in-memory `papers` array, no full page reload, so `beforeunload` never fires for its own ◀ ▶); `dataset.html` keeps its existing one-record-per-page-load model — including its own edit lock acquired/released per load — so ◀ ▶ does a **real navigation** (`window.location.href`) to reuse that lock machinery rather than building a parallel in-place-swap path. That means `beforeunload`'s native prompt wouldn't fire until navigation *already* started, too late to offer a save — so `goToAdjacentDataset()` checks `isDirty` itself first: a plain `confirm()` ("You have unsaved changes. Save them before continuing?") — OK saves (via the existing `save()`) then navigates, only if the save actually succeeds (`isDirty` false afterward); Cancel — or a failed save — keeps the user on the page rather than risking a silent discard. No new modal component; this is the same plain-`confirm()` register as the rest of the page's unsaved-changes handling (the native `beforeunload` dialog for tab-close/Back/breadcrumb navigation). Adding the nav row made `.overview-header` span the full container width (breadcrumb pushed left, ◀ ▶ + Account pushed right via `justify-content: space-between`), while `.detail-form`'s existing `max-width: 560px` left the form and "Used in Papers" sitting flush-left with visible empty space to their right — a width mismatch against the now-wider header. Fixed by adding an `overview-detail-nav` class to `dataset.html`'s own `<div class="overview">` and scoping the override to it (`.overview-detail-nav .detail-form { max-width: none; }`) rather than changing `.detail-form` itself, since `metric.html` shares that class and has no ◀ ▶ nav widening its header — confirmed `metric.html`'s form is still capped at 560px.
- **On Modal / Correspondence** (issue #104, `datasets.on_modal`/`datasets.correspondence` — `repro-sign-survey-backend` PR #57): two more three-way radios on the dataset detail page, same markup/wiring pattern as `available` (`populateForm()`/`save()`/`setReadOnly()`/`wireEvents()` in `dataset-detail.js` each gained a matching branch), each with an `.info-btn`/`.info-popup` tooltip (same pattern as other field tooltips) since the short labels alone don't fully explain the field — "On Modal": "Whether this dataset is already stored on the compute platform Modal.com."; "Correspondence": "Status of reaching out to this dataset's original owners, e.g. about access or licensing." (Labels were originally the fuller "Stored on Modal.com" / "Correspondence with Data Owners" with no tooltip; shortened once a tooltip was added, to declutter the form.) `on_modal` is `yes` / `no` / `""` ("Not answered"), matching `available`'s convention exactly. `correspondence` is `contacted_got_reply` / `contacted_waiting` / `""` — deliberately in that order (most-progressed state first), unlike `available`/`on_modal` which go affirmative → negative → unanswered. Note the empty string means something different here than on `available`/`on_modal`: per the backend PR, PocketBase selects have no per-field defaults, so `""` had to double as "not contacted yet" rather than adding a literal `not_contacted_yet` value (the issue's original spec), to avoid two different values meaning the same thing. The frontend labels that empty-string option "Not contacted" for this field specifically, rather than reusing the "Not answered" wording used elsewhere — the backend PR explicitly flagged that the UI needs to label the shared empty state differently per field. Radio labels themselves are deliberately terse — "Got reply" / "Awaiting reply" / "Not contacted" rather than repeating "Contacted, " on the first two — since that prefix was redundant (both already imply contact was made) and made the row visually crowded. Neither field is required to save (mirrors `available`, which has never been required either).
- **Metrics catalogue** (`metrics-index.html`): table listing all records from the `metrics` collection (name, first URL, comments). Clicking a row navigates to `metric.html?id=<pb-id>`. "+ Add Metric" navigates to a blank `metric.html`. The detail page (`metric.html`) has fields for name, URLs (chip list), and comments, with the same edit locking pattern.
- **Unsaved-changes guard** (`dataset.html`/`metric.html`): these pages have no autosave — a Save button persists edits explicitly — so an `isDirty` flag is set by any field mutation (name/license/comments input, availability radio, URL chip add/remove) and cleared on load and after a successful save. `beforeunload` shows the browser's native "leave site?" prompt (`e.preventDefault(); e.returnValue = ''`) whenever `isDirty` is true, covering both actual tab close/reload and in-app navigation (e.g. the Back link), since these pages do a full page load rather than SPA-style routing. Applies equally to a brand-new, not-yet-saved record (`record === null`). While dirty, the lock release + heartbeat-stop that normally runs in `beforeunload` is skipped — releasing the lock immediately would be wrong if the user cancels the prompt and keeps editing — so a lock left behind by a user who dismisses the prompt and leaves anyway just expires via the existing 30-minute inactivity rule instead of being released early. `dataset.html`'s ◀ ▶ navigation (see below) has its own, separate unsaved-changes handling — a `confirm()` checked *before* triggering navigation — since it needs an explicit save step this generic `beforeunload` guard can't offer.
- **Used in Papers** (dataset/metric detail pages only, not for a new/unsaved record): a scrollable, always-fully-shown list of every paper referencing this dataset/metric — title (links to `paper.html?id=<paper-id>` in a new tab) plus a status badge, same class/text mapping as the Reviewing overview table. Computed entirely client-side (`pbGetAll('papers')` filtered by `p.datasets`/`p.metrics` array membership), matching the Review Stats page's approach — no backend relation-filter query involved, so no risk from PocketBase's `~` operator not being an exact array-membership match (confirmed empirically it under-counts for this use case). Loads in the background after the form/lock, doesn't block page interactivity.
- **Persistence**: Save sends a PATCH to the appropriate collection endpoint on PocketBase. Two IDs per record: `paper_id` (kebab slug used in URLs and display) mapped to `p.id`; opaque PocketBase `id` stored as `p._pb_id` and used only for API calls.
- **Pagination**: `pbGetAll(collection)` in `api.js` pages through PocketBase results until all records are loaded, avoiding the 500-record ceiling. Both overview and detail scripts use this helper.
- **Edit locking**: opening a paper acquires a lock by setting `locked_by` + `locked_at` on the record. A heartbeat PATCH fires every 60 s to keep the lock alive. On save, navigation, or tab close the lock is released (`locked_by: ""`). If PATCH returns 404, the paper is locked by another user — the form goes read-only with a notice banner. Lock expiry (30 min of inactivity) is enforced client-side only.

## Testing

CI runs on every push/PR to main. To run locally:

```bash
python3 -m py_compile server.py          # Python syntax
python3 scripts/validate_data.py         # JSON schema
npm run validate:html                    # HTML validation
PB_TEST_EMAIL=<email> PB_TEST_PASSWORD=<password> npx playwright test
```

Playwright tests require a running PocketBase backend and a valid user account. Pass credentials via environment variables — store them in a local `.env` file (gitignored) and source it, or pass inline as above. Without those variables the Playwright tests are skipped rather than failed (so CI still passes). The shared `test.beforeEach()` hook injects the same `pb_token`/`pb_user_id`/`pb_email`/`pb_token_expiry` localStorage keys a real login sets (see Auth, below) — `pb_email` was missing for most of this project's history, which silently made `getEmail()` return falsy throughout the whole suite until the Assignees feature (#108) started actually depending on it.

**Tests run on their own dedicated port (8766), separate from your own `server.py` (8765)**: `playwright.config.js` runs the test server on port 8766, never 8765, specifically so a Playwright run never touches (or is confused by) a `server.py` you're running by hand for everyday manual use — and, in the other direction, so you starting/stopping your own instance can never interfere with a test run in progress. This was added after `webServer.reuseExistingServer` (true outside CI) once silently reused a stale `server.py` left over from manual `curl` testing that predated a config change (`PB_URL` hadn't been in `webServer.env` yet when it was started) — the suite ran against the wrong PocketBase with no obvious error, surfaced while building `/export`: by-id lookups 404'd for records that demonstrably existed, because that process's `/export` was quietly talking to the *production* backend, and the invalid-token-on-production case came back empty rather than erroring (see the JSON export endpoint section above). The same class of staleness is still theoretically possible on 8766 itself (e.g. an interrupted previous test run) — if a test run behaves inconsistently between isolated (`-g "..."`) and full runs, `lsof -i :8766` and kill anything already there; never touch whatever's on 8765, since that's someone's own browsing session.

Playwright tests cover (93 tests total):
- **Landing**: task cards render (5 cards), all task links present, no card links to Checking (internal/unlisted for now)
- **Review overview**: renders list/controls, search filters live, empty state, row click → `paper.html`, search clear button appears while typing and resets the search on click
- **Review detail**: core UI elements, autosave persists a field change without finalizing (and the `Saved ✓` indicator stays visible rather than auto-hiding), the Comments field is optional and autosaves, a second autosave visibly cycles back through `Saving…` rather than staying stuck on `Saved ✓`, a network failure during autosave shows "Save failed" instead of hanging on "Saving…" forever, Finalize disabled until required fields are filled then marks paper as Final (skipped if no datasets/metrics in backend; restores mutated fields afterward), "Create unnamed dataset" adds a distinctly-named chip and does not require Comments (#92), Sub-area of SLP's suggestions switch between the Recognition and Translation pools (and go empty, not hidden) as Area of SLP changes, a free-text Sub-area chip persists across reload, and it's not required for Finalize (#101), Status History logs flag/clear transitions newest-first, clearing a flag/rejection also clears its reason (#63), ◀ ▶ navigation updates URL, back link → `review-index.html`, Copy Link strips the active nav filter, filtered-row click carries `q`/`status` into the URL and constrains ◀ ▶ to that subset / clicking the explicit "Review →" link also carries the filter (#91) / unfiltered click and direct navigation use the full collection / Back restores the same filter / a non-matching `q` self-heals to the full collection (#75), Download JSON downloads just this one paper via `/export`, keyed by its `paper_id` slug
- **Check overview**: renders list/controls, row click → `paper-check.html`, search clear button appears while typing and resets the search on click
- **Check detail**: core UI elements including both radio groups, back link → `check-index.html`, clearing a flag also clears its reason (#63), Copy Link strips the active nav filter, filtered-row click carries `q`/`status` into the URL and constrains ◀ ▶ to that subset / clicking the explicit "Check →" link also carries the filter (#91) / unfiltered click and direct navigation use the full collection / Back restores the same filter (#75)
- **Datasets overview**: renders table and controls (+ Add link), row click → `dataset.html` (skipped if no records), `#stats-row` matches the extended on-Modal/used-in-final/got-reply format (#106), the filter bar renders all seven controls and the result count stays hidden unfiltered (#106), the On Modal/Correspondence columns exist and every row remaining after filtering Correspondence to "Awaiting reply" visibly shows that same badge (#106), the Assignees column shows only the first of two assignees and the "Only assigned to me" filter narrows every remaining row to that same email (#108), search filters live and shows "X of Y datasets" then clears back to an unfiltered URL, the Orphans filter's "only"/"hide" counts sum to the unfiltered total and the Final-paper filter narrows the list (#106), a `<select>` set away from "All" gets the `.active` highlight while the other five stay neutral and it clears back to unstyled at "All" (#106), Clear filters is disabled with nothing active, resets the search box and every `<select>` once something is (#106), and the active filters round-trip through the "Details →" link into `dataset.html`'s URL and back out through both the Back link and the "Datasets" breadcrumb, restoring the same `<select>` value (#106)
- **Dataset detail**: new dataset page loads form fields (including the `on_modal`/`correspondence` radios and an empty Assignees with "Assign myself" showing) and back link, and (having no place in any nav order) shows ◀ ▶ disabled with a "—" counter (#106); "Assign myself"/"Remove myself" toggles the current user's email in and out of `assignees`, persists across a reload, and is restored to its original state afterward (#108); the "On Modal"/"Correspondence" radios default to unanswered, save, and survive a reload (#104 — self-skips if the backend schema doesn't have these fields yet, e.g. before `repro-sign-survey-backend` PR #57 is deployed); existing dataset shows the Used in Papers section; editing a field triggers the unsaved-changes guard on `beforeunload` and saving clears it; a new (unsaved) dataset also triggers the guard; ◀ ▶ steps through the same filtered selection opened from `datasets-index.html`, with the counter and disabled-at-the-ends state correct at both boundaries (#106); and clicking ▶ with unsaved changes prompts to save — Cancel keeps the edit and stays put, OK persists it (verified against the backend) and then navigates (#106)
- **Metrics overview**: renders table and controls (+ Add link), row click → `metric.html` (skipped if no records)
- **Metric detail**: new metric page loads form fields and back link, existing metric shows the Used in Papers section, editing a field triggers the unsaved-changes guard on `beforeunload` and saving clears it
- **Review Stats page**: renders all breakdown sections (summary, status breakdown, sub-area-of-SLP section, answered-fields table), the availability badge doesn't misalign the bar tracks in Top Datasets, clickable Top Datasets/Metrics labels are visually distinct (accent blue at rest) from non-clickable ones and open in a new tab, reachable from the landing page, the subnav highlights the current page and navigates to both other stats pages (#103)
- **Dataset Stats page**: reachable via the subnav from the Review Stats page, renders the summary and all breakdown sections (Availability/On Modal/Correspondence/Top Assignees), the three breakdowns' counts each sum to the dataset total — including the "Unanswered"/"Not contacted" rows, which regression-guards the `tally()` empty-string-key bug found while building this page (#103); the "Used in a Final paper" toggle defaults to unchecked (all datasets), reduces the summary total and every breakdown's counts to match when switched on, gets the `.active` highlight, syncs into the URL, and restores on direct navigation
- **Reproduction Stats page**: reachable via the subnav from the Review Stats page, renders the summary and all breakdown sections (Reproduction Status/Top Assignees/All Datasets Available/All Datasets On Modal), the status breakdown's counts sum to the final-papers total including "Not started" — same regression guard as Dataset Stats (#103)
- **Reproduction Tracker page**: reachable from the landing page, renders the table structure and all four filter `<select>`s plus the search box via direct navigation, a filter selection is mirrored into the URL and gets the `.active` highlight then restores on direct navigation, the Assigned-to filter's "anyone"/"nobody" options partition the full paper list (counts sum to the unfiltered total), row click and the explicit "Details →" link both navigate to `reproduction.html` in the same tab carrying the active filter, the detail page renders the read-only Paper block (ID/Title/Open-in-Reviewing link/a Name-Available-On Modal datasets table) and the editable form (status radios, Assign button, Save, Back link) with Download JSON already enabled; Download JSON downloads the reproduction for a paper that has one (asserting `expand.paper.paper_id` round-trips back to the id downloaded with) and shows a clear `alert()` for a paper that doesn't (#110); assigning myself, setting status to In progress, and adding a URL on a paper with no existing reproduction row creates one (verified via a direct API check), persists across a reload, and is neutralized back to empty afterward (`reproductions` has no delete API, so a test/mistake row can't be removed, only reset to the same empty state the app already treats as "not started") (#110); a dedicated regression test confirms saving a reproduction never touches the paper's own `locked_by`/`locked_at` fields, proving the two collections' locks stay independent (#110, backend PR #62)
- **`/export` endpoint**: missing/unknown `collection` and a missing `Authorization` header each get the right status code, an `id` containing a quote is rejected before ever reaching a PocketBase filter; full-collection exports (`datasets`, `papers`) come back with `locked_by`/`locked_at` stripped from every record — and from `papers`' expanded `datasets`/`metrics` too; a single dataset by PocketBase id and a single paper by `paper_id` each match the record fetched directly from PocketBase and 404 for an unknown id; a single reproduction by its paper's `paper_id` (asserting `expand.paper.paper_id` round-trips back to the id queried with) matches the record fetched directly from PocketBase, 404s for an unknown id, and — for a reproduction whose paper has datasets/metrics — asserts the double expansion: `expand.paper.expand.datasets`/`expand.paper.expand.metrics` come back as full inline records (not bare ids), each with `locked_by`/`locked_at` stripped too
## Adding papers

Papers are managed in the PocketBase backend (see backend repo). The frontend reads all records from `/api/collections/papers/records` (Reviewing) and `/api/collections/check_papers/records` (Checking). The `data.json` in this repo is reference seed data used only by CI schema validation — it is not read by the frontend.
